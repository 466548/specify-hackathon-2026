# Specify frontend environment variables
# 実際の値は frontend/.env.local にコピーして使う（コミットしない）。

# backend の公開先。ローカルは通常このまま。
BACKEND_URL=http://localhost:8000

# backend と共有する分析 API 保護用トークン。
SPECIFY_ANALYZE_TOKEN=
